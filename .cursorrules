# Cursor Rules & Guidelines

Este proyecto de LaTeX modular sigue pautas estrictas para mantener la consistencia, modularidad y cumplimiento de la normativa del Bachillerato Técnico Humanístico (BTH - RM 0912/2023) en Bolivia bajo la modalidad de **Innovación Tecnológica**.

Antes de comenzar cualquier tarea de desarrollo o modificación de código, lee las directrices del proyecto en el archivo principal `AGENTS.md` y la estructura de capítulos en `ESTRUCTURA_CAPITULOS.md`.

## Resumen de Reglas Clave:
1. **Variables Centralizadas:** Toda modificación de metadatos (autor(es), título, tutor, institución, especialidad, modalidad) y espaciado de párrafos (`\espacioposteriorparrafo`, `\sangriaprimeralinea`) debe realizarse únicamente en `estilos/configuracion.tex`. Soporta 1 o 2 autores dinámicamente (`\autoruno`, `\autordos`). NUNCA hardcodear nombres o datos en los `.tex`.
2. **Estructura Modular (Innovación Tecnológica):** El proyecto contiene 9 capítulos en el directorio `capitulos/` (`01_introduccion/` a `09_conclusiones_recomendaciones/`). Cada carpeta incluye un `main.tex` que ensambla las secciones. Rutas internas: `\input{capitulos/0X_.../...}`.
3. **Estilo de Títulos APA 7ma Edición Adaptado:**
   - Todos los títulos y enlaces internos en **negro** (`linkcolor=black`).
   - Interlineado sencillo (`1.0` / `\setstretch{1.0}`) para títulos de Nivel 1 al 4.
   - Nivel 1 (`\chapter`): Alineado a la **izquierda**, sin prefijo de palabra "Capítulo" (ej: `1. INTRODUCCIÓN`), y con espaciado anterior a `-15pt` en `titlesec`.
   - Nivel 2 (`\section`): Alineado izquierda, Negrita.
   - Nivel 3 (`\subsection`): Alineado izquierda, Negrita y Cursiva.
   - Nivel 4 (`\subsubsection`): Sangría de 1.27 cm, Negrita, tipo *run-in* terminando con punto.
   - Nivel 5 (`\paragraph`): Sangría de 1.27 cm, Negrita y Cursiva, tipo *run-in* terminando con punto.
4. **Encabezados y Pies de Página:**
   - Sin textos de cabecera ni línea horizontal superior (`headrulewidth=0pt`).
   - Numeración de página arábiga centrada abajo (`\cfoot{\thepage}`) en Capítulos 1 al 9.
   - Preliminares con numeración romana (`\pagenumbering{roman}`).
   - Secciones finales (Bibliografía/Anexos) sin numeración en títulos (`numberless`) y sin numeración de página (`\pagestyle{empty}`).
5. **Bibliografía APA 7ma Edición:** Motor `biblatex` con estilo `apa` y backend `biber`. Usar `\parencite{clave}` para citas parentéticas y `\textcite{clave}` para narrativas sobre `bibliografia/referencias.bib`. Lista generada con `\printbibliography`.
6. **Código Fuente y Programación:** Entorno `listings` con estilo `estilocodigo` predeterminado y tipografía Courier. Guardar programas en `codigo/` e importar con `\lstinputlisting[language=...]{codigo/archivo.ext}` (o bloques `lstlisting`).
7. **Tablas e Ilustraciones:** Guardar tablas modulares en `tablas/` e importar vía `\input{tablas/archivo.tex}`. Seguir APA 7: usar `booktabs` (`\toprule`, `\midrule`, `\bottomrule`), PROHIBIDO líneas verticales (`|`) y `\hline`, `\caption` siempre arriba. Para textos largos usar `tabularx` (columnas `L`, `C`, `R`, `X`) que respeten `\textwidth`. Auditar con `./compilar.sh --check-tablas`. Figuras en `imagenes/` e incluir con `\includegraphics{archivo.ext}`.
8. **Control de Silabación:** Desactivada globalmente (`\hyphenpenalty=10000`, `\exhyphenpenalty=10000`).
9. **Compilación y Limpieza:** Usa exclusivamente el script ejecutable `./compilar.sh` (`--clean`, `--only-clean`, `--fast`, `--check-tablas`).
10. **Modalidad y Prompts:** Modalidad fija en Innovación Tecnológica (cargada en `main.tex` vía `\input{capitulos/index.tex}`). Ficha de datos en `docs/ficha-proyecto.md` (alimentada desde `docs/proyecto.rtf` o `docs/proyecto.md` y consultas al usuario). Prompts de migración y redacción en `promts/migracion/` (`ficha-proyecto.md`), y suite de revisión académica en `promts/revicion/` (16 prompts adaptados a los 9 capítulos de Innovación Tecnológica). Páginas preliminares `agradecimiento.tex` y `dedicatoria.tex` comparten formato (derecha, cursiva, `\vspace*{3cm}`). Redacción formal y comentarios en español (excepto abstract).
11. **Formato Numérico (SI/ISO 80000-1):** Punto decimal obligatorio (ej. 12.50, 98.5%, 0.75). PROHIBIDO usar coma (,) en decimales. PROHIBIDO usar comas ni puntos como separadores de millares (evitar 1,000 y 1.000); escribir 4 cifras juntas (ej. 4500.00, 1000) y 5 o más cifras con espacio o continuas (ej. 25 000.00 o 25000.00).

